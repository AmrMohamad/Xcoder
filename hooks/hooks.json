{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/usr/bin/python3 -c 'import glob, os, runpy, sys; paths = glob.glob(os.path.expanduser(\"~/.codex/plugins/cache/*/xcode/0.3.0/scripts/xcode_gui_first_prompt_hook.py\")); sys.exit(\"xcode gui-first prompt hook not found\") if not paths else runpy.run_path(paths[0], run_name=\"__main__\")'",
            "timeout": 5,
            "statusMessage": "Applying Xcode GUI-first routing"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "^Bash$",
        "hooks": [
          {
            "type": "command",
            "command": "/usr/bin/python3 -c 'import glob, os, runpy, sys; paths = glob.glob(os.path.expanduser(\"~/.codex/plugins/cache/*/xcode/0.3.0/scripts/xcode_gui_first_pre_tool_hook.py\")); sys.exit(\"xcode gui-first pre-tool hook not found\") if not paths else runpy.run_path(paths[0], run_name=\"__main__\")'",
            "timeout": 5,
            "statusMessage": "Checking Xcode GUI-first routing"
          }
        ]
      }
    ]
  }
}
